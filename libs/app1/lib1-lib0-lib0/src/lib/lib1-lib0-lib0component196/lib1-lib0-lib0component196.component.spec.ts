import { ComponentFixture, TestBed } from '@angular/core/testing';

import { Lib1Lib0Lib0component196Component } from './lib1-lib0-lib0component196.component';

describe('Lib1Lib0Lib0component196Component', () => {
  let component: Lib1Lib0Lib0component196Component;
  let fixture: ComponentFixture<Lib1Lib0Lib0component196Component>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ Lib1Lib0Lib0component196Component ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(Lib1Lib0Lib0component196Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
