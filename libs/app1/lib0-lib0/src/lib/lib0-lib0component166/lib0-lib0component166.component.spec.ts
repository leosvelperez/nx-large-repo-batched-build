import { ComponentFixture, TestBed } from '@angular/core/testing';

import { Lib0Lib0component166Component } from './lib0-lib0component166.component';

describe('Lib0Lib0component166Component', () => {
  let component: Lib0Lib0component166Component;
  let fixture: ComponentFixture<Lib0Lib0component166Component>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ Lib0Lib0component166Component ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(Lib0Lib0component166Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
