import { ComponentFixture, TestBed } from '@angular/core/testing';

import { Lib0Lib0component186Component } from './lib0-lib0component186.component';

describe('Lib0Lib0component186Component', () => {
  let component: Lib0Lib0component186Component;
  let fixture: ComponentFixture<Lib0Lib0component186Component>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ Lib0Lib0component186Component ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(Lib0Lib0component186Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
