import { ComponentFixture, TestBed } from '@angular/core/testing';

import { Lib1Lib1Lib1component167Component } from './lib1-lib1-lib1component167.component';

describe('Lib1Lib1Lib1component167Component', () => {
  let component: Lib1Lib1Lib1component167Component;
  let fixture: ComponentFixture<Lib1Lib1Lib1component167Component>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ Lib1Lib1Lib1component167Component ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(Lib1Lib1Lib1component167Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
